# electron-forge (apps/mac) requires a hoisted node_modules so electron-packager
# can traverse and prune the app's dependency tree — pnpm's default isolated
# (symlinked) layout breaks packaging. hoisted changes only the on-disk layout,
# not the committed lockfile. see CLAUDE.md → "packaging the mac app".
node-linker=hoisted
